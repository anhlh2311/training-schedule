# Web push decision

This project uses **Firebase Cloud Messaging (FCM)** for web push. See [FCM_WEB_PUSH.md](FCM_WEB_PUSH.md) for setup and troubleshooting.

Alternatives (e.g. third-party engagement platforms) were evaluated; FCM is free for messaging, aligns with the existing Firebase stack, and keeps a single service worker.
